import React from 'react';
import { Box } from '@mui/material';

const Nomenclature: React.FC = () => (
  <Box sx={{ padding: 2, backgroundColor: '#fff' }}>
    <p>Контент для раздела "Номенклатура".</p>
  </Box>
);

export default Nomenclature;
