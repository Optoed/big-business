import React from 'react';
import { Box } from '@mui/material';

const Requests: React.FC = () => (
  <Box sx={{ padding: 2, backgroundColor: '#fff' }}>
    <p>Контент для раздела "Запросы".</p>
  </Box>
);

export default Requests;
