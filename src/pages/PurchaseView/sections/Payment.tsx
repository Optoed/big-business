import React from 'react';
import { Box } from '@mui/material';

const Payment: React.FC = () => (
  <Box sx={{ padding: 2, backgroundColor: '#fff' }}>
    <p>Контент для раздела "Оплата".</p>
  </Box>
);

export default Payment;
